{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_runner",
            "command": "flutter pub run build_runner build --delete-conflicting-outputs",
            "type": "shell"
        }
    ]
}